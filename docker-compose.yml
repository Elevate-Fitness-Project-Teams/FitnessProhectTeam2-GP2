services:
  sqlserver-db:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: elevate-sqlserver-db
    restart: unless-stopped
    ports:
      - "1444:1433"
    environment:
      - ACCEPT_EULA=Y
      - MSSQL_SA_PASSWORD=Elvate2026!
    volumes:
      - sqlserver-db-data:/var/opt/mssql

  # --- MariaDB (Nutrition DB) ---
  nutrition-db:
    image: mariadb:11.4
    container_name: elevate-nutrition-db
    restart: unless-stopped
    ports:
      - "3307:3306"
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: nutrition
      MYSQL_USER: nutrition_user
      MYSQL_PASSWORD: nutrition_pass
    volumes:
      - nutrition-db-data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 10s
      timeout: 5s
      retries: 5
  # --- RabbitMQ Message Broker ---
  rabbitmq:
    image: rabbitmq:3.13-management-alpine
    container_name: elevate-rabbitmq
    restart: unless-stopped
    ports:
      - "5672:5672"   
      - "15672:15672" # Management Dashboard
    environment:
      - RABBITMQ_DEFAULT_USER=guest
      - RABBITMQ_DEFAULT_PASS=guest
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "check_running"]
      interval: 10s
      timeout: 5s
      retries: 5
  # --- Auth Microservice ---
  elevate.auth:
    build:
      context: .
      dockerfile: src/services/Elevate.Auth/Dockerfile
    image: elevate.auth
    container_name: elevate-auth-api
    restart: unless-stopped
    ports:
      - "5007:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__DefaultConnection=Server=sqlserver-db,1433;Database=ElevateAuthDb;User Id=sa;Password=Elvate2026!;TrustServerCertificate=True;
      - JwtSettings__Secret=SuperSecretKeyThatIsVeryLongAndSecure123456!
      - MessageBroker__Host=rabbitmq
    depends_on:
      rabbitmq:
        condition: service_healthy
  # --- Nutrition Microservice ---
  nutrition-api:
    build:
      context: .
      dockerfile: src/services/Elevate.Nutrition/Dockerfile
    image: elevate.nutrition
    container_name: elevate-nutrition-api
    restart: unless-stopped
    ports:
      - "5003:8080"
    depends_on:
      nutrition-db:
        condition: service_healthy
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__NutritionDb=server=nutrition-db;port=3306;database=nutrition;user=nutrition_user;password=nutrition_pass;CharSet=utf8mb4
      - Services__FceApi__BaseUrl=http://elevate.fitnesscalculation:8080
      - JwtSettings__Secret=SuperSecretKeyThatIsVeryLongAndSecure123456!
      - JwtSettings__Issuer=ElevateAuthService
      - JwtSettings__Audience=ElevateFitnessApp

  elevate.fitnesscalculation:
    image: elevate.fitnesscalculation
    build:
      context: .
      dockerfile: src/services/FitnessCalculation/Dockerfile

  elevate.notifications:
    image: elevate.notifications
    build:
      context: .
      dockerfile: src/services/Elevate.Notifications/Dockerfile
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - MessageBroker__Host=rabbitmq
  elevate.profile:
    image: elevate.profile
    build:
      context: .
      dockerfile: src/services/Elevate.Profile/Dockerfile
    container_name: elevate-profile-api
    restart: unless-stopped
    ports:
      - "5010:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__DefaultConnection=Server=sqlserver-db,1433;Database=Profileservice;User Id=sa;Password=Elvate2026!;TrustServerCertificate=True;
      - JwtSettings__Secret=SuperSecretKeyThatIsVeryLongAndSecure123456!
      - MessageBroker__Host=rabbitmq
    depends_on:
      rabbitmq:
        condition: service_healthy
  elevate.progress:
    image: elevate.progress
    build:
      context: .
      dockerfile: src/services/Elevate.Progress/Dockerfile
    container_name: elevate-progress-api
    restart: unless-stopped
    ports:
      - "5011:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__DefaultConnection=Server=sqlserver-db,1433;Database=ElevateProgressDb;User Id=sa;Password=Elvate2026!;TrustServerCertificate=True;
      - JwtSettings__Secret=SuperSecretKeyThatIsVeryLongAndSecure123456!
      - MessageBroker__Host=rabbitmq
    depends_on:
      rabbitmq:
        condition: service_healthy

  elevate.workout:
    image: elevate.workout
    build:
      context: .
      dockerfile: src/services/Elevate.Workout/Dockerfile
    container_name: elevate-workout-api
    restart: unless-stopped
    ports:
      - "5012:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__DefaultConnection=Server=sqlserver-db,1433;Database=ElevateWorkoutDb;User Id=sa;Password=Elvate2026!;TrustServerCertificate=True;
      - JwtSettings__Secret=SuperSecretKeyThatIsVeryLongAndSecure123456!
      - MessageBroker__Host=rabbitmq
    depends_on:
      rabbitmq:
        condition: service_healthy

  elevate.subscription:
    image: elevate.subscription
    build:
      context: .
      dockerfile: src/services/Elevate.subscription/Dockerfile
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__DefaultConnection=Server=elevate-sqlserver-db,1433;Database=ElevateAuthDb;User Id=sa;Password=Elvate2026!;TrustServerCertificate=True;
      - JwtSettings__Secret=SuperSecretKeyThatIsVeryLongAndSecure123456!
      - MessageBroker__Host=rabbitmq
    depends_on:
      rabbitmq:
        condition: service_healthy

volumes:
  sqlserver-db-data:
  nutrition-db-data:
 
  

